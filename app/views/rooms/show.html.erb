<p style="color: green"><%= notice %></p>

<%= render @room %>

<div>
  <%= link_to t('links.edit_room'), edit_room_path(@room) %> |
  <%= link_to t('links.back_to_rooms'), rooms_path %>

  <%= button_to t('links.delete'), @room, method: :delete %>
</div>
